defmodule Dijkstra do
  # def should_go_left(curr_cord, end_cord, last_move) do
  #   curr_cord[:x] > end_cord[:x]
  # end

  # def should_go_right(last_curr_cordcord, end_cord, last_move) do
  #   curr_cord[:x] < end_cord[:x]
  # end

  # def should_go_top(curr_cord, end_cord, last_move) do
  #   curr_cord[:y] > end_cord[:y]
  # end

  # def should_go_bottom(curr_cord, end_cord, last_move) do
  #   curr_cord[:y] < end_cord[:y]
  # end

  # def equal(cord1, cord2) do
  #   cord1[:y] < cord2[:y] && cord1[:x] < cord2[:x]
  # end

  # defp find(start_cord, end_cord) do
  #   if(equal())
  # end
end
